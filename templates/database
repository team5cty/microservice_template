package database

import (
	"database/sql"

	_ "github.com/lib/pq"
)

func Conn() (*sql.DB, error) {
	databaseurl := "{{.Url}}"
	db, err := sql.Open("postgres", databaseurl)
	if err != nil {
		return nil, err
	}
	return db, nil
}
