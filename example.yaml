database:
  provider: postgres
  url: postgresql://postgres:l@localhost/mydb

endpoints: 
  - name: User
    path: /user/{id}
    method: GET
    schema:
        type: object
        properties:
          id: int
          username: string
          email: string
          password: string

  - path: /users
    method: GET
    schema:
        type: list
        properties:
          username: string
          email: string

  - path: /getrandomuser
    method: GET


