module: example_output_module

database:
  provider: postgres
  url: postgresql://postgres:password@localhost:3000/newdb5
  models:
    - table: User
      schema:
        id: Int @id @default(autoincrement())
        username: String 
        email: String 
    - table: Random
      schema:
        id: Int @id 

endpoints: 
  - name: Users
    path: /user/{id}
    method: GET
    table: User
    json:
      type: list
      properties:
    

  - name: User
    path: /user/
    method: GET
    table: User
    json:
        type: object
        properties:
          username: string
          email: string

  - name: AddUser
    path: /adduser/{id}
    method: POST
    table: User
    json:
        type: object
        properties:
          


