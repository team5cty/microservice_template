#As specified in https://www.prisma.io/docs/orm/reference/connection-urls
database:
  provider: postgres
  url: postgresql://postgres:l@localhost/mydb

#models: models:
    # - name: "User"
    #   fields:
    #     - name: id
    #       type: Int @id @default(autoincrement())
    #     - name: "username"
    #       type: "String @db.Text"
    #     - name: "email"
    #       type: "String @db.VarChar(255)"

endpoints: 
  - path: /user/{id}
    method: GET
    schema:
    #here in schema we have to specify which model it is referring
    #   model: User
        type: object
        properties:
          id: int
          username: string
          email: string
          password: string

  - path: /users
    method: GET
    schema:
        type: list
        properties:
          username: string
          email: string

  - path: /getrandomuser
    method: GET
    # If there is no schema, handler function for /getrandomuser should be empty
    # and will be defined by user.


